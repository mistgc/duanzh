# Example secret - in production, create this with:
# kubectl create secret generic duanzh-secrets \
#   --from-literal=llm-api-key=<your-api-key>
apiVersion: v1
kind: Secret
metadata:
  name: duanzh-secrets
type: Opaque
data:
  # This is intentionally empty - create with kubectl
  llm-api-key: # Add actual value with kubectl